# App Scaffold

This folder is reserved for the future app layer that consumes the language
bundles.

## Expected responsibilities

- Select a language bundle
- Load verse lookup JSON
- Load dictionary/search data
- Support RTL display
- Keep language-specific assets separated

## Suggested next structure

```text
app/
├── public/
│   └── data/
├── src/
└── README.md
```

For now, the repo only needs the placeholder so the path exists for future
updates.

